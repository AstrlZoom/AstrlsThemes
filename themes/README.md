# Themes

`my-discord-theme.theme.css` is the editable source theme file.

Use it for AstrlGlass metadata, color variables, transparency settings, and DMs icon bounce settings.

For manual installation, run `pnpm build` and use `dist/my-discord-theme.theme.css`.
